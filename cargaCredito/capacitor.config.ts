import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'carga_credito',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
