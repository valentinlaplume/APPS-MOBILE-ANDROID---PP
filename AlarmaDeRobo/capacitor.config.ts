import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alarmaRobo.app',
  appName: 'alarma-robo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
